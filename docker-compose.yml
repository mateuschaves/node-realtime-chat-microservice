version: '3.8'

services:
  messenger:
    build: .
    container_name: 'messenger'
    deploy:
      restart_policy:
        condition: on-failure
    ports:
      - '2000:3000'
    volumes:
      - .:/usr/app
    links:
      - database
  database:
    image: mongo:latest
    ports:
      - '3333:3333'
    container_name: 'messenger_database'
